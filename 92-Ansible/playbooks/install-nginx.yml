---
- name: Install nginx
  hosts: "{{ target_host }}"
  become: true
  become_method: sudo
  become_user: root

  tasks:
    - name: Install nginx
      ansible.builtin.apt:
        name: nginx
        state: present
        update_cache: false
